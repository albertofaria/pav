# ---------------------------------------------------------------------------- #

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc
  labels:
    pav-test-object: ''
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  volumeName: my-pv

# ---------------------------------------------------------------------------- #
